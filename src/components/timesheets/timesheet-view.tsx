import Link from "next/link";
import { CalendarDays, Plus } from "lucide-react";
import { PageHeader } from "@/components/ui/page-header";
import { formatDuration } from "@/lib/utils";

export type TimesheetEntry={id:string;date:string;description:string;project:string;color:string;startedAt:Date;endedAt:Date;durationMinutes:number;billable:boolean;status:string};
export function TimesheetView({entries}:{entries:TimesheetEntry[]}){return <><PageHeader title="Timesheets" description="Review and organize your logged time." actions={<Link className="btn btn-primary" href="/timer"><Plus size={15}/>Add time</Link>}/>{!entries.length?<section className="card grid min-h-[400px] place-items-center p-6 text-center"><div className="max-w-sm"><span className="mx-auto grid size-14 place-items-center rounded-2xl bg-[var(--accent-soft)] text-[var(--accent)]"><CalendarDays size={24}/></span><h2 className="mt-5 text-xl font-bold">Your timesheet is empty</h2><p className="muted mt-2 text-sm leading-6">Once you record your first entry, it will appear here for review.</p><Link href="/timer" className="btn btn-primary mt-5"><Plus size={15}/>Add first entry</Link></div></section>:<div className="card overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-[13px]"><thead className="bg-[var(--surface-2)] text-[11px] text-[var(--muted)] uppercase"><tr>{["Date","Description","Project","Time","Duration","Billable","Status"].map(label=><th key={label} className="px-4 py-3 font-semibold">{label}</th>)}</tr></thead><tbody>{entries.map(entry=><tr key={entry.id} className="border-t border-[var(--border)]"><td className="px-4 py-3.5">{new Intl.DateTimeFormat("en",{month:"short",day:"numeric",year:"numeric"}).format(new Date(`${entry.date}T00:00:00Z`))}</td><td className="max-w-[260px] truncate px-4 py-3.5 font-medium">{entry.description}</td><td className="px-4 py-3.5"><span className="flex items-center gap-2"><i className="size-2 rounded-full" style={{background:entry.color}}/>{entry.project}</span></td><td className="muted px-4 py-3.5">{new Intl.DateTimeFormat("en",{hour:"numeric",minute:"2-digit",timeZone:"UTC"}).format(new Date(entry.startedAt))} – {new Intl.DateTimeFormat("en",{hour:"numeric",minute:"2-digit",timeZone:"UTC"}).format(new Date(entry.endedAt))}</td><td className="px-4 py-3.5 font-semibold">{formatDuration(entry.durationMinutes)}</td><td className="px-4 py-3.5">{entry.billable?"Yes":"No"}</td><td className="px-4 py-3.5"><span className="badge capitalize">{entry.status}</span></td></tr>)}</tbody></table></div></div>}</>}
