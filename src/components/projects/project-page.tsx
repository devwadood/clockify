import Link from "next/link";
import { and, desc, eq, isNull } from "drizzle-orm";
import { notFound } from "next/navigation";
import { CalendarDays, Plus } from "lucide-react";
import { getDb } from "@/db";
import { projectMembers, projects, timeEntries, user } from "@/db/schema";
import { requireUser } from "@/lib/auth/session";
import { formatDuration, initials } from "@/lib/utils";

export async function ProjectPage({projectId,tab="overview"}:{projectId:string;tab?:string}){const current=await requireUser();const db=getDb();const [project]=await db.select({id:projects.id,name:projects.name,client:projects.clientName,code:projects.code,color:projects.color,status:projects.status,description:projects.description,role:projectMembers.role}).from(projectMembers).innerJoin(projects,eq(projects.id,projectMembers.projectId)).where(and(eq(projectMembers.userId,current.id),eq(projects.id,projectId),isNull(projectMembers.revokedAt),isNull(projects.deletedAt))).limit(1);if(!project)notFound();const [entries,members]=await Promise.all([db.select({id:timeEntries.id,description:timeEntries.description,startedAt:timeEntries.startedAt,durationMinutes:timeEntries.durationMinutes,status:timeEntries.status}).from(timeEntries).where(and(eq(timeEntries.projectId,project.id),isNull(timeEntries.deletedAt),project.role==="member"?eq(timeEntries.userId,current.id):undefined)).orderBy(desc(timeEntries.startedAt)).limit(20),db.select({id:user.id,name:user.name,email:user.email,role:projectMembers.role}).from(projectMembers).innerJoin(user,eq(user.id,projectMembers.userId)).where(and(eq(projectMembers.projectId,project.id),isNull(projectMembers.revokedAt)))]);const base=`/projects/${project.id}`;return <><section className="card mb-5 p-5"><div className="flex items-center gap-3"><span className="grid size-12 place-items-center rounded-xl text-lg font-bold text-white" style={{background:project.color}}>{project.name[0]}</span><div className="min-w-0 flex-1"><h1 className="truncate text-2xl font-bold">{project.name}</h1><p className="muted mt-1 text-xs">{[project.client,project.code].filter(Boolean).join(" · ")||"Personal project"}</p></div><span className="badge capitalize">{project.role}</span></div><nav className="-mb-5 mt-6 flex gap-5 overflow-auto">{[["overview","Overview"],["timesheets","Time entries"],["members","Members"],["reports","Reports"],["settings","Settings"]].map(([id,label])=><Link key={id} href={id==="overview"?base:`${base}/${id}`} className={`border-b-2 pb-3 text-xs font-semibold ${tab===id?"border-[var(--accent)] text-[var(--accent)]":"muted border-transparent"}`}>{label}</Link>)}</nav></section>{tab==="members"?<section className="card overflow-hidden">{members.map(member=><div key={member.id} className="flex items-center gap-3 border-b border-[var(--border)] p-4 last:border-0"><span className="grid size-9 place-items-center rounded-lg bg-[var(--surface-2)] text-xs font-bold">{initials(member.name)}</span><div className="min-w-0 flex-1"><b className="block truncate text-sm">{member.name}</b><p className="muted truncate text-xs">{member.email}</p></div><span className="badge capitalize">{member.role}</span></div>)}</section>:tab==="settings"?<section className="card p-5"><h2 className="section-title">Project settings</h2><p className="muted mt-2 text-sm">Status: <span className="capitalize">{project.status}</span></p><p className="muted mt-4 text-sm leading-6">{project.description||"No project description has been added."}</p></section>:<section className="card overflow-hidden"><div className="flex items-center justify-between border-b border-[var(--border)] p-5"><div><h2 className="section-title">Recorded time</h2><p className="muted mt-1 text-xs">{formatDuration(entries.reduce((n,e)=>n+e.durationMinutes,0))} shown</p></div><Link href="/timer" className="btn btn-primary"><Plus size={14}/>Add time</Link></div>{entries.length?entries.map(entry=><div key={entry.id} className="flex items-center gap-3 border-b border-[var(--border)] p-4 last:border-0"><CalendarDays size={16} className="muted"/><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{entry.description}</p><p className="muted mt-1 text-xs">{new Intl.DateTimeFormat("en",{dateStyle:"medium"}).format(entry.startedAt)}</p></div><b className="text-sm">{formatDuration(entry.durationMinutes)}</b></div>):<div className="muted p-10 text-center text-sm">No time has been recorded for this project.</div>}</section>}</>}
