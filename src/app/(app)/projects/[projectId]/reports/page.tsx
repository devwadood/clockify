import { ProjectPage } from "@/components/projects/project-page"; export const dynamic="force-dynamic"; export default async function Page({params}:{params:Promise<{projectId:string}>}){return <ProjectPage projectId={(await params).projectId} tab="reports"/>}
